<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
    <f:entry title="${%Display name}">
        <f:textbox name="portlet.name" field="name" default="${descriptor.getDisplayName()}" />
    </f:entry>
    <j:invokeStatic var="allJobs" className="org.korosoft.jenkins.plugin.rtp.RichTextPortlet" method="getAllJobNames"/>
    <f:entry title="Job name">
        <f:editableComboBox field="jobName" items="${allJobs}" />
    </f:entry>
</j:jelly>